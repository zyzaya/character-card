import React from 'react';
import '../../styles/AttackSave.css';

export default function AttackSave(props) {
  return (
    <div className="attack_save">
      {`+ __ to hit, save DC __, WIS + LVL prepared (__)`}
    </div>
  );
}
